# Go Service Profile

- Process entry points and assembly owner: `{{SERVICE_ENTRY_POINTS}}`
- Domain, transport, persistence, and dependency boundaries:
  `{{SERVICE_BOUNDARIES}}`
- Database, migration, and generated-protocol authorities:
  `{{SERVICE_AUTHORITIES}}`
- Unit, integration, real-system, and complete verification:
  `{{GO_SERVICE_CHECKS}}`
- Build artifact, runtime environment, deployment, rollback, and release owner:
  `{{GO_SERVICE_DELIVERY}}`
