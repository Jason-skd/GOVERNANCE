# Bun Web Profile

- Runtime and package-manager versions: `{{BUN_AND_RUNTIME_VERSIONS}}`
- Application, component, state, API-client, and generated-type boundaries:
  `{{WEB_BOUNDARIES}}`
- Dependency lock and generated API authority: `{{WEB_AUTHORITIES}}`
- Install, format, lint, type-check, test, and production-build commands:
  `{{BUN_WEB_CHECKS}}`
- Static artifact, hosting environment, deployment, and release owner:
  `{{BUN_WEB_DELIVERY}}`

Use Bun commands and lockfiles consistently when Bun is the selected package
manager; remove conflicting package-manager metadata during initialization.
